services:
  - type: web
    name: miliastra-docs
    runtime: node
    rootDir: docs-site
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
